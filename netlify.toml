[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"

# Configuración para manejar redirects de SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 